# Automation & Orchestration

- **Pipelines / orchestration:** `bash`/`python` scripts to combine `amass -> httpx -> nuclei -> aquatone`.
- `nuclei` + `nuclei-templates` for templated checks.
- `metabigor`, `metabigor-x` (asset correlation).
- `Golang` / `Python` one-off tools for custom workflows.