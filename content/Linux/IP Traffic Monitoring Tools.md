
### `iptraf-ng` (TCP/IP connection visualizer)
```
sudo apt install iptraf-ng
sudo iptraf-ng
```
![[Pasted image 20260724001613.png]]

### `tcptrack` (live TCP connection tracker)
```
sudo apt install tcptrack
sudo tcptrack -i eth0
```
![[Pasted image 20260724003400.png]]
